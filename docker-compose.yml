version: '3.4'

services:
  sunswitcher:
    image: jorie70/sunswitcher:0.0.5
    container_name: sunswitcher
    restart: always
    environment:
      - "TZ=Europe/Berlin"
    logging:
      driver: "json-file"
      options:
        max-size: "10M"
        max-file: "10"
